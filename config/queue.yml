default: &default
  clear_finished_jobs_after: 1.day # 追加: 1日で完了ジョブを自動削除
  dispatchers:
    - polling_interval: 5
      batch_size: 15
  workers:
    - queues: "*"
      threads: 2
      processes: 1
      polling_interval: 5

development:
  <<: *default

production:
  <<: *default
