default: &default
  dispatchers:
    - polling_interval: 5
      batch_size: 100
  workers:
    - queues: "*"
      threads: 2
      processes: 1
      polling_interval: 5

development:
  <<: *default

production:
  <<: *default